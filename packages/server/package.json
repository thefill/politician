{
    "name": "@politician/server",
    "description": "Active mock server",
    "license": "MIT",
    "version": "0.0.1",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "build:watch": "npm-watch build",
        "clean": "rimraf dist"
    },
    "watch": {
        "build": {
            "patterns": [
                "src"
            ],
            "extensions": "ts",
            "quiet": false
        }
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "cors": "^2.8.4",
        "express": "^4.16.3",
        "route-pattern": "0.0.6",
        "chalk": "^2.4.1"
    },
    "devDependencies": {
        "@types/express": "^4.16.0"
    },
    "engines": {
        "node": ">=6.1"
    },
    "author": "Filip Dabrowski",
    "repository": {
        "type": "git",
        "url": "git@github.com:thefill/politician.git"
    }
}
